import Layout from "@shared/components/layout";
import AuthWindow from "@widgets/authWindow";

function Auth() {
  return (
    <Layout className="flex justify-center items-center">
      <AuthWindow />
    </Layout>
  )
}

export default Auth